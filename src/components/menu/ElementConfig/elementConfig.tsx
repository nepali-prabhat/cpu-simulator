import { paletteWidth } from "@/constants";

export const ElementConfig = () => {
    return (
        <section className="p-2" style={{ width: paletteWidth }}>
            TODO: element config options. Inputs, background, stroke, etc
        </section>
    );
};
